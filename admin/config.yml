backend:
  name: github
  repo: jinjcovsky/jinjcovsky.github.io
  branch: main
  base_url: https://woistjan-proxy.jkienast-at.workers.dev

media_folder: "assets/uploads"
public_folder: "/assets/uploads"

collections:
  - name: "blog"
    label: "Blog"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    format: "frontmatter"
    fields:
      - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Latitude", name: "lat", widget: "number", value_type: "float", required: false}
      - {label: "Longitude", name: "lon", widget: "number", value_type: "float", required: false}
      - {label: "Body", name: "body", widget: "markdown"}
